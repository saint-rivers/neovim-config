require("core.keymap")
require("core.set")
require("core.lazy")
require("core.plugins")

-- require("lspconfig").gopls.setup({
--     on_attach = function()
--         vim.keymap.set("n", "K", vim.lsp.buf.hover, { buffer = 0 })
--     end
-- })
